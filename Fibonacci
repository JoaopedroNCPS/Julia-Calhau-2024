#include <stdio.h>

int main() {
    int a, b, resultado;
    b = 1;
    a = 0;
    resultado = a + b;
    printf("resultado = %d\n", resultado);
    a = 1;
    for (int i = 0; i < 10; ++i) {
        resultado = a + b;
        printf("resultado = %d\n", resultado);
        a = b;
        b = resultado;
    }
    return 0;
}
